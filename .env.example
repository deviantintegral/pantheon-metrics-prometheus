# Pantheon Machine Tokens
# Required: Space-separated list of Pantheon machine tokens for authentication
# Get tokens from: https://dashboard.pantheon.io/account/machine-tokens
PANTHEON_MACHINE_TOKENS=your-token-here another-token-here

# Pantheon Environment
# Optional: Which Pantheon environment to monitor (default: live)
# Options: live, test, dev, or any multidev environment name
PANTHEON_ENV=live

# Refresh Interval
# Optional: How often to refresh site metrics in minutes (default: 60)
REFRESH_INTERVAL=60

# Grafana Configuration
# Optional: Grafana admin credentials (default: admin/admin)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
